/**
 * Caption management config: buffer size, auto-clear interval, silence threshold.
 */
export const MAX_CAPTION_LINES = 3;
export const AUTO_CLEAR_MS = 10 * 1000; // 10 seconds
export const SILENCE_CLEAR_MS = 3000;  // 3 seconds of no new caption -> clear
