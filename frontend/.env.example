# Voice Translation Frontend - Environment Variables
# Copy to .env when customizing.

# Backend API base URL (optional; in dev Vite proxy is used)
# VITE_API_URL=http://localhost:8000

# WebSocket URL (optional; defaults to same host + /ws/audio)
# VITE_WS_URL=ws://localhost:8000/ws/audio
